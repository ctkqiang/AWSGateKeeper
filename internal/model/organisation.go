// Package model (organisation.go) defines the Organisation domain entity.
package model

// Organisation represents a business unit or company within the
// identity governance model.
type Organisation struct {
	ID   string
	Name string
}
